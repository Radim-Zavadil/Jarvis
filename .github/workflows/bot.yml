name: Telegram Bot

on:
  schedule:
    # Morning briefing — 7:00 AM CET (UTC+1, winter): 06:00 UTC
    - cron: '0 6 * * *'
    # Morning briefing — 7:00 AM CEST (UTC+2, summer): 05:00 UTC
    - cron: '0 5 * * *'
    # F1 session checker — Fri/Sat/Sun only, every 30 min (covers all sessions globally)
    - cron: '*/30 0-23 * * 5,6,0'
  workflow_dispatch:
    inputs:
      mode:
        description: 'Mode to run (morning or f1_check)'
        required: true
        default: 'morning'
        type: choice
        options:
          - morning
          - f1_check

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Determine mode
        id: mode
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            if [ "${{ github.event.inputs.mode }}" = "morning" ]; then
              echo "mode=morning --force" >> "$GITHUB_OUTPUT"
            else
              echo "mode=${{ github.event.inputs.mode }}" >> "$GITHUB_OUTPUT"
            fi
          elif [ "${{ github.event.schedule }}" = "0 6 * * *" ] || [ "${{ github.event.schedule }}" = "0 5 * * *" ]; then
            # Daily morning cron (winter/summer UTC shifts)
            echo "mode=morning" >> "$GITHUB_OUTPUT"
          else
            # 30-minute F1 sesson checker cron (or any other trigger)
            echo "mode=f1_check" >> "$GITHUB_OUTPUT"
          fi

      - name: Run bot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY }}
          GOOGLE_CALENDAR_CREDENTIALS: ${{ secrets.GOOGLE_CALENDAR_CREDENTIALS }}
          GOOGLE_TOKEN_JSON: ${{ secrets.GOOGLE_TOKEN_JSON }}
        run: python main.py ${{ steps.mode.outputs.mode }}
